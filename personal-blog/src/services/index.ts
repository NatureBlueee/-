/**
 * 服务层统一导出
 *
 * 使用方式：
 * import { getArticles, getArticleBySlug } from '@/services';
 */

export {
  getArticles,
  getArticleBySlug,
  getAllArticleSlugs,
  getRecentArticles,
} from './crossbell';
